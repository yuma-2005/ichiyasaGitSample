//二つの変数を初期化して表示

#include <stdio.h>

int main(void)
{
    int x = 3.14;  // xはint型の変数 (3.14で初期化？)
    int y = 5.7;   // yはint型の変数　(5.7で初期化？)

    printf("xの値は%dです。\n", x);  //xの値を表示
    printf("yの値は%dです。\n", y);  //yの値を表示

    return 0;
}